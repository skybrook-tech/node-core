import errorHandler from "./error-handler";
import response from "./response";
import authentication from "./authentication";

const defaults = { errorHandler, response, authentication };

export default defaults;
